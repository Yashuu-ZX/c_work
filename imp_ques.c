#include <stdio.h>
int main()
{
    for (int i = 5; i ; --i)
    
    {
        printf("%d\n",i);
    }
    //any non-zero number in c means true so until i become zero it runs the code
    return 0;
}